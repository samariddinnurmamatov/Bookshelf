const API_HOST = "https://no23.lavina.tech";

const appConfig = {
  apiPrefix: API_HOST,
  authenticatedEntryPath: "/main",
  unAuthenticatedEntryPath: "/sign-in",
  tourPath: "",
  enableMock: false,
};

export default appConfig;
